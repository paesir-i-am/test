# 프로젝트 설정 가이드

## 📦 설치 및 실행

### 1. 의존성 설치
```bash
npm install
```

### 2. 환경 변수 설정
`.env.example` 파일을 참고하여 `.env.local` 파일을 생성하세요.

```bash
cp .env.example .env.local
```

`.env.local` 파일을 편집하여 API URL을 설정하세요:
```
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001/api/v1
```

### 3. 개발 서버 실행
```bash
npm run dev
```

브라우저에서 [http://localhost:3000](http://localhost:3000)을 열어 확인하세요.

## 📁 프로젝트 구조

```
sooka/
├── app/                    # Next.js App Router
│   ├── (auth)/            # 인증 관련 페이지
│   │   ├── login/         # 로그인
│   │   └── register/      # 회원가입
│   ├── vehicles/          # 차량 관련 페이지
│   ├── layout.tsx         # 루트 레이아웃
│   ├── page.tsx           # 홈페이지
│   └── globals.css        # 전역 스타일
├── components/            # 재사용 가능한 컴포넌트
│   ├── layout/           # 레이아웃 컴포넌트
│   │   ├── Header.tsx    # 헤더
│   │   └── Footer.tsx    # 푸터
│   └── vehicle/         # 차량 관련 컴포넌트
│       └── VehicleCard.tsx
├── lib/                  # 유틸리티 및 API
│   ├── api.ts           # API 클라이언트
│   └── utils.ts         # 유틸리티 함수
├── store/               # 상태 관리 (Zustand)
│   └── authStore.ts     # 인증 스토어
├── types/               # TypeScript 타입 정의
│   ├── user.ts
│   └── vehicle.ts
├── public/              # 정적 파일
│   └── manifest.json     # PWA 매니페스트
└── docs/                 # 프로젝트 문서
```

## 🎨 스타일링

이 프로젝트는 **Tailwind CSS**를 사용합니다.

- 디자인 시스템: `tailwind.config.ts`에서 색상, 간격, 브레이크포인트 등 정의
- 반응형: 모바일 우선 설계 (Mobile First)
- 브레이크포인트:
  - 모바일: 0px
  - 태블릿: 768px
  - 데스크톱: 1024px

## 🔧 주요 기능

### 구현 완료
- ✅ 프로젝트 기본 구조
- ✅ Next.js 14 + TypeScript 설정
- ✅ Tailwind CSS 설정
- ✅ PWA 기본 설정
- ✅ 인증 페이지 (로그인/회원가입)
- ✅ 차량 목록 페이지
- ✅ 반응형 레이아웃 (헤더/푸터)
- ✅ 상태 관리 (Zustand)

### 구현 필요
- 차량 상세 페이지
- 차량 등록 페이지
- 찜하기 기능
- 문의 기능
- 관리자 페이지
- 프로필 관리

## 📱 PWA 설정

PWA는 프로덕션 빌드에서만 활성화됩니다.

개발 환경에서 테스트하려면:
```bash
npm run build
npm start
```

PWA 아이콘 파일 (`public/icon-192x192.png`, `public/icon-512x512.png`)을 추가해야 합니다.

## 🔗 API 연동

백엔드 API가 준비되면:
1. `.env.local` 파일에서 `NEXT_PUBLIC_API_BASE_URL` 설정
2. `lib/api.ts`에서 API 엔드포인트 확인
3. API 응답 형식이 문서(`docs/05_API_명세서.md`)와 일치하는지 확인

## 📚 문서 참고

자세한 내용은 `docs/` 폴더의 문서를 참고하세요:
- `01_프로젝트_개요서.md`: 프로젝트 전체 개요
- `02_요구사항_정의서.md`: 기능 요구사항
- `03_기능_명세서.md`: 기능 상세 명세
- `05_API_명세서.md`: API 엔드포인트 명세
- `06_UI_UX_디자인_가이드라인.md`: 디자인 가이드

## 🐛 문제 해결

### 의존성 설치 오류
```bash
rm -rf node_modules package-lock.json
npm install
```

### TypeScript 오류
```bash
npm run type-check
```

### 빌드 오류
```bash
rm -rf .next
npm run build
```

